class Product(object):
    """Represents a Product class."""
    def __init__(self, product_name, url, limit):
        self.product_name = product_name
        self.url = url
        self.limit = limit


